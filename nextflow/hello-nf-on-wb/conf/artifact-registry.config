// A withName selector overrides the container in this process's module.
// Enable with -profile docker,artifact_registry or workbench,artifact_registry.
process {
    withName: convertToUpper {
        container = params.upper_container
    }
}
